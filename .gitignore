node_modules
dist
webapp/dist
webapp/node_modules
webapp/*.tsbuildinfo
webapp/vite.config.js
webapp/vite.config.d.ts
.env
.env.local
*.log
data/
generated/
prisma/migrations/dev.db*
.DS_Store
coverage/

# Court-directory source screenshots (OCR input) — local only, never
# committed. The court directory is already baked into
# src/templates/court-info.generated.ts, so this folder isn't needed
# at runtime or for builds.
info sud/

# video build intermediates
video/_work/
video/_work_uz/
video/_work_pro/
video/_photos/
